#include <stdio.h>
#include<string.h>
int main() 
{
   char a[20];
   int length = 0;
   int i;
   printf("enter any string:");
   scanf("%s", &a);
   for(i = 0 ; a[i]!='\0' ; i ++)
   {
       length ++;
   }
   printf("length = %d", length);
   return 0;
}